{
    "$schema": "https://apify.com/schemas/v1/input.ide.json",
    "title": "PissedConsumer Review Scraper",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "companyUrls": {
            "title": "Company URLs or slugs",
            "type": "array",
            "description": "PissedConsumer company URLs (e.g. https://amazon.pissedconsumer.com/review.html) or just company slugs (e.g. amazon).",
            "editor": "stringList",
            "prefill": ["amazon"]
        },
        "maxReviewsPerCompany": {
            "title": "Max reviews per company",
            "type": "integer",
            "description": "Maximum number of reviews to extract per company. Set 0 for unlimited.",
            "default": 100,
            "minimum": 0
        },
        "sortBy": {
            "title": "Sort reviews by",
            "type": "string",
            "description": "How to sort the reviews.",
            "editor": "select",
            "enum": ["helpful", "latest"],
            "default": "latest"
        },
        "filterByStars": {
            "title": "Filter by star rating",
            "type": "string",
            "description": "Only extract reviews with this star rating. 'all' means no filter.",
            "editor": "select",
            "enum": ["all", "1", "2", "3", "4", "5"],
            "default": "all"
        },
        "includeCompanyInfo": {
            "title": "Include company profile info",
            "type": "boolean",
            "description": "Include a company profile summary (average rating, total reviews, star distribution) as the first result for each company.",
            "default": true
        },
        "proxyConfig": {
            "title": "Proxy configuration",
            "type": "object",
            "description": "Select proxies to use. Apify Proxy is recommended.",
            "editor": "proxy",
            "default": { "useApifyProxy": true }
        }
    },
    "required": ["companyUrls"]
}
